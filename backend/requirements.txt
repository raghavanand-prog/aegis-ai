# AEGISX backend
# Pinned to the versions the project was developed and tested against.

fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
alembic==1.19.1
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
PyJWT==2.13.0
python-multipart==0.0.32

# PostgreSQL driver (binary wheels, no local libpq build required)
psycopg[binary]==3.3.5

# --- V3: machine learning --------------------------------------------------
# scikit-learn for Isolation Forest; joblib for artifact serialization. Pinned
# because a model artifact is only reproducible against the version that wrote
# it, and the loader verifies the artifact digest.
scikit-learn==1.7.2
numpy==2.3.4
joblib==1.5.2

# --- V3: outbound HTTP -----------------------------------------------------
# Used by the threat-intelligence and AI provider clients. Already present for
# the test suite; promoted to a runtime dependency in V3.

httpx==0.28.1

# Development / test only
pytest==9.1.1
