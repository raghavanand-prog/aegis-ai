"""Candidate models: trained, registered, and unable to serve.

A candidate exists as an artifact and a registry row. It reaches production only
through evaluation, safety gates and an administrator's approval - never by
being trained, and never because it scored well.
"""
