# Superseded by [tool.pytest.ini_options] in pyproject.toml; kept because the
# sandbox that generated it cannot delete files. Safe to remove.
[pytest]
testpaths = app/tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
